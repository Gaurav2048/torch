{
  "scripts": {
    "dev_client": "cd ./client && npm install && npm run dev",
    "dev_server": "cd ./server && npm install && npm run start"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "socket.io": "^4.7.5"
  }
}
